# Platform configuration for this application
# Generated by Backstage scaffolder
# DO NOT edit manually - managed via GitOps

app:
  name: test-leo
  namespace: test-leo
  description: Test install via backstage
  owner: group:default/guests

build:
  arch: arm64
  platforms: linux/arm64
  ecrRepository: idp-test-leo
  tagStrategy: sha

deployment:
  replicas: 1
  size: small
  exposePublic: true
  scheduling:

    nodeSelector:
      kubernetes.io/arch: arm64


platform:
  cluster: platform-eks
  region: us-east-2
  domain: g2p.timedevops.click
