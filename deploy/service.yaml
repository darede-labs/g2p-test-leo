apiVersion: v1
kind: Service
metadata:
  name: test-leo
  namespace: test-leo
  labels:
    app: test-leo
    app.kubernetes.io/name: test-leo
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app: test-leo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
